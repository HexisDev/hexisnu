<div class="pad-div", style="background: url(<%=asset_path('bgs/1.jpg') %>) no-repeat center center fixed"></div>

<div class="pad-div tourn-pad-div" style="background: unset">
	<div class="row tourn-row">
		<div class="col-sm-10 col-sm-offset-1">
		  <% @tournaments.order(name: :desc).each do |tournament| %>
				<a href="<%= tournament_path(tournament) %>" class="tourn-btn">
					<button class="btn btn-lg btn-primary"><%= tournament.name %></button>
				</a>
	    <% end %>
		</div>
	</div>
</div>